version: '3'

services:
  nginx:
    volumes:
      - writings-hq_sync:/var/www:nocopy
  php-fpm:
    volumes:
      - writings-hq_sync:/var/www:nocopy
  workspace:
    volumes:
      - writings-hq_sync:/var/www:nocopy

volumes:
  writings-hq_sync:
    external: true
