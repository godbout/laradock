version: '3'

services:
  nginx:
    volumes:
      - company-hq-sync:/var/www:nocopy
  php-fpm:
    volumes:
      - company-hq-sync:/var/www:nocopy
  workspace:
    volumes:
      - company-hq-sync:/var/www:nocopy

volumes:
  company-hq-sync:
    external: true
